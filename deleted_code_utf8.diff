commit 357fb7ffa64838b7b76d43a734a9ef1ded40df2f
Author: lucky kumar <lk5485029@gmail.com>
Date:   Sun Jun 7 17:06:31 2026 +0530

    Remove video player, remove watch history, revert title to FilmiFied, add profile bio feature

diff --git a/app.js b/app.js
index d22d39e..3e5bd0e 100644
--- a/app.js
+++ b/app.js
@@ -16,7 +16,6 @@ const RATINGS = [
 let genres = [], activeGenre = null, mediaType = 'all', currentPage = 1;
 let heroMovies = [], heroIdx = 0, heroInterval;
 let modalMovieId = null, modalMediaType = 'movie', selectedRating = null;
-
 /* ===== INIT ===== */
 document.addEventListener('DOMContentLoaded', init);
 
@@ -487,32 +486,7 @@ function renderModal(d, credits, prov, isWatched) {
 
   let html = '';
 
-  // ===== WATCH NOW PLAYER =====
-  if (modalMediaType === 'movie') {
-    html += `<div class="mb-section player-section">
-      <h3>Γû╢∩╕Å Watch Now</h3>
-      <button class="watch-btn" onclick="togglePlayer()">≡ƒÄ¼ Play Movie</button>
-      <div class="player-wrap hidden" id="playerWrap">
-        <iframe id="videoPlayer" src="" frameborder="0" allowfullscreen allow="autoplay; encrypted-media" class="video-iframe"></iframe>
-      </div>
-    </div>`;
-  } else {
-    // Series ΓÇö season & episode selectors
-    const totalSeasons = d.number_of_seasons || 1;
-    let seasonOpts = '';
-    for (let s = 1; s <= totalSeasons; s++) seasonOpts += `<option value="${s}">Season ${s}</option>`;
-    html += `<div class="mb-section player-section">
-      <h3>Γû╢∩╕Å Watch Now</h3>
-      <div class="series-controls">
-        <select class="series-select" id="seasonSelect" onchange="loadEpisodes()">${seasonOpts}</select>
-        <select class="series-select" id="episodeSelect"><option value="1">Episode 1</option></select>
-        <button class="watch-btn" onclick="playEpisode()">≡ƒÄ¼ Play Episode</button>
-      </div>
-      <div class="player-wrap hidden" id="playerWrap">
-        <iframe id="videoPlayer" src="" frameborder="0" allowfullscreen allow="autoplay; encrypted-media" class="video-iframe"></iframe>
-      </div>
-    </div>`;
-  }
+
 
   // Songs (placeholder ΓÇö filled async via Deezer)
   html += `<div class="mb-section" id="songsSection"><h3>≡ƒÄ╡ Songs & Soundtrack</h3><div class="songs-loading">Loading songs...</div></div>`;
@@ -585,8 +559,7 @@ function renderModal(d, credits, prov, isWatched) {
 
   loadAndRenderCloudReviews(modalMovieId);
 
-  // If series, auto-load episodes for season 1
-  if (modalMediaType === 'tv') loadEpisodes();
+  // Player removed
 
   // Fetch songs async
   fetchSongs(title);
@@ -815,52 +788,6 @@ function previewSong(btn, url) {
   previewAudio.onended = () => { btn.classList.remove('playing'); btn.textContent = 'Γû╢'; previewAudio = null; };
 }
 
-/* ===== VIDEO PLAYER ===== */
-function togglePlayer() {
-  const wrap = document.getElementById('playerWrap');
-  const iframe = document.getElementById('videoPlayer');
-  if (wrap.classList.contains('hidden')) {
-    iframe.src = `https://www.vidking.net/embed/movie/${modalMovieId}?color=e50914&autoPlay=true`;
-    wrap.classList.remove('hidden');
-    wrap.scrollIntoView({ behavior: 'smooth', block: 'center' });
-    if (window.saveWatchHistory && window.currentMovieData) {
-      window.saveWatchHistory(window.currentMovieData);
-    }
-  } else {
-    iframe.src = '';
-    wrap.classList.add('hidden');
-  }
-}
-
-async function loadEpisodes() {
-  const season = document.getElementById('seasonSelect').value;
-  const epSelect = document.getElementById('episodeSelect');
-  try {
-    const data = await tmdb(`/tv/${modalMovieId}/season/${season}`);
-    const eps = data.episodes || [];
-    epSelect.innerHTML = eps.map(e => `<option value="${e.episode_number}">Ep ${e.episode_number} ΓÇö ${e.name || ''}</option>`).join('');
-    if (!eps.length) epSelect.innerHTML = '<option value="1">Episode 1</option>';
-  } catch {
-    epSelect.innerHTML = '<option value="1">Episode 1</option>';
-  }
-}
-
-function playEpisode() {
-  const season = document.getElementById('seasonSelect').value;
-  const episode = document.getElementById('episodeSelect').value;
-  const wrap = document.getElementById('playerWrap');
-  const iframe = document.getElementById('videoPlayer');
-  iframe.src = `https://www.vidking.net/embed/tv/${modalMovieId}/${season}/${episode}?color=e50914&autoPlay=true&nextEpisode=true&episodeSelector=true`;
-  wrap.classList.remove('hidden');
-  wrap.scrollIntoView({ behavior: 'smooth', block: 'center' });
-  if (window.saveWatchHistory && window.currentMovieData) {
-    window.saveWatchHistory({
-      ...window.currentMovieData,
-      season: season,
-      episode: episode
-    });
-  }
-}
 
 /* ===== INDIAN CINEMA HUB & GENRE SPOTLIGHT ===== */
 
@@ -1283,34 +1210,7 @@ function closeModal() {
 
 /* ===== CONTINUE WATCHING ===== */
 window.renderContinueWatching = function(history) {
-  const section = document.getElementById('continueWatchingSection');
-  const scroll = document.getElementById('continueWatchingScroll');
-  
-  if (!section || !scroll) return;
-  
-  if (!history || history.length === 0) {
-    section.classList.add('hidden');
-    scroll.innerHTML = '';
-    return;
-  }
-  
-  section.classList.remove('hidden');
-  
-  scroll.innerHTML = history.map(item => {
-    const isTv = item.mediaType === 'tv';
-    const epBadge = isTv && item.season ? `<div class="cw-badge">S${item.season} E${item.episode}</div>` : '';
-    
-    // The click handler redirects to movie.html (or auto-plays episode logic)
-    return `
-      <div class="cinema-card" onclick="openModal(${item.id}, '${item.mediaType}')">
-        <img src="${item.poster || 'data:image/svg+xml,<svg/>'}" alt="${item.title}" loading="lazy"/>
-        ${epBadge}
-        <div class="cinema-card-info">
-          <h4>${item.title}</h4>
-        </div>
-      </div>
-    `;
-  }).join('');
+  // Continue watching removed
 };
 function closeModalOnOverlay(e) { if (e.target === document.getElementById('modalOverlay')) closeModal(); }
 
diff --git a/db.js b/db.js
index 17e1717..2760d00 100644
--- a/db.js
+++ b/db.js
@@ -230,6 +230,19 @@ window.updateProfilePhoto = async function(photoUrl) {
   }
 };
 
+window.updateBio = async function(bioText) {
+  const user = window.getCurrentUser ? window.getCurrentUser() : null;
+  if (!db || !user) return false;
+  
+  try {
+    await db.collection('users').doc(user.uid).update({ bio: bioText });
+    return true;
+  } catch (e) {
+    console.error("Error updating bio:", e);
+    return false;
+  }
+};
+
 window.getPublicProfile = async function(uid) {
   if (!db) return null;
   try {
diff --git a/index.html b/index.html
index eaa61ef..7058267 100644
--- a/index.html
+++ b/index.html
@@ -3,7 +3,7 @@
 <head>
   <meta charset="UTF-8"/>
   <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
-  <meta name="description" content="FilmiFied - too much yapping,not enough cinema"/>
+  <meta name="description" content="FilmiFied - Your Personal Movie Universe"/>
   <title>FilmiFied ≡ƒÄ¼</title>
   <link rel="preconnect" href="https://fonts.googleapis.com"/>
   <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;600;700;800;900&family=Bebas+Neue&display=swap" rel="stylesheet"/>
@@ -32,7 +32,7 @@
     </svg>
     <div class="logo-text-wrap">
       <span class="logo-title">FilmiFied</span>
-      <span class="logo-tagline">too much yapping,not enough cinema</span>
+      <span class="logo-tagline">Too Much Yapping Not Enough Cinema</span>
     </div>
   </div>
   <div class="search-wrap">
@@ -69,15 +69,7 @@
 <!-- ===== GENRE PILLS ===== -->
 <main class="main" id="mainContent">
 
-  <!-- ===== CONTINUE WATCHING ===== -->
-  <section class="cinema-section hidden" id="continueWatchingSection" style="margin-top: 20px;">
-    <div class="cinema-section-header">
-      <h2 class="cinema-section-title">
-        <span style="font-size: 1.5rem">Γû╢∩╕Å</span> Continue Watching
-      </h2>
-    </div>
-    <div class="cinema-scroll" id="continueWatchingScroll"></div>
-  </section>
+
 
   <!-- ===== GENRE SPOTLIGHT ===== -->
   <section class="genre-spotlight-section">
diff --git a/movie.html b/movie.html
index 5370a96..35325cb 100644
--- a/movie.html
+++ b/movie.html
@@ -40,7 +40,7 @@
     </svg>
     <div class="logo-text-wrap">
       <span class="logo-title">FilmiFied</span>
-      <span class="logo-tagline">too much yapping,not enough cinema</span>
+      <span class="logo-tagline">Too Much Yapping Not Enough Cinema</span>
     </div>
   </div>
   <div class="search-wrap">
diff --git a/person.html b/person.html
index 0dbedcd..985fdbc 100644
--- a/person.html
+++ b/person.html
@@ -48,7 +48,7 @@
       </svg>
       <div class="logo-text-wrap">
         <span class="logo-title">FilmiFied</span>
-        <span class="logo-tagline">too much yapping,not enough cinema</span>
+        <span class="logo-tagline">Too Much Yapping Not Enough Cinema</span>
       </div>
     </div>
     <div class="search-wrap">
diff --git a/profile.html b/profile.html
index 88579a5..d228a57 100644
--- a/profile.html
+++ b/profile.html
@@ -136,7 +136,7 @@
     </svg>
     <div class="logo-text-wrap">
       <span class="logo-title">FilmiFied</span>
-      <span class="logo-tagline">too much yapping,not enough cinema</span>
+      <span class="logo-tagline">Too Much Yapping Not Enough Cinema</span>
     </div>
   </div>
   <div class="nav-right">
@@ -158,6 +158,17 @@
       </div>
       <div class="profile-info">
         <h1 class="profile-name" id="profileName">Loading...</h1>
+        <div class="profile-bio-container" id="bioContainer" style="display: none;">
+          <p class="profile-bio-text" id="bioText"></p>
+          <button class="edit-bio-btn" id="editBioBtn" onclick="toggleBioEdit()" style="display: none;">Edit Bio</button>
+        </div>
+        <div class="profile-bio-edit" id="bioEditContainer" style="display: none;">
+          <textarea id="bioInput" class="bio-textarea" placeholder="Write something about your cinematic taste..." maxlength="150"></textarea>
+          <div class="bio-actions">
+            <button class="btn-primary" onclick="saveBio()" style="padding: 6px 12px; font-size: 0.8rem;">Save</button>
+            <button class="btn-ghost" onclick="toggleBioEdit()" style="padding: 6px 12px; font-size: 0.8rem;">Cancel</button>
+          </div>
+        </div>
       </div>
     </div>
   </div>
@@ -165,7 +176,7 @@
   <div class="profile-tabs" id="profileTabs">
     <button class="ptab active" onclick="switchTab('reviewsTab', this)">≡ƒô¥ Reviews</button>
     <button class="ptab" onclick="switchTab('watchedTab', this)">≡ƒÄ¼ Watched</button>
-    <button class="ptab" onclick="switchTab('historyTab', this)" id="historyTabBtn">Γû╢∩╕Å History (Private)</button>
+
   </div>
 
   <div id="reviewsTab" class="tab-content active">
@@ -176,9 +187,7 @@
     <div class="history-grid" id="userWatchedContainer"><div class="empty-state">No movies marked as watched yet.</div></div>
   </div>
 
-  <div id="historyTab" class="tab-content">
-    <div class="history-grid" id="userHistoryContainer"><div class="empty-state">Loading watch history...</div></div>
-  </div>
+
 </div>
 
 <!-- ===== NOTIFICATION ===== -->
@@ -224,7 +233,7 @@
     }
 
     if (!isOwnProfile) {
-      document.getElementById('historyTabBtn').style.display = 'none';
+
       document.getElementById('avatarOverlay').style.display = 'none';
       document.getElementById('profileAvatar').style.cursor = 'default';
     }
@@ -237,6 +246,26 @@
     }
 
     document.getElementById('profileName').textContent = profileData.username || 'User';
+
+    // Bio handling
+    const bioText = document.getElementById('bioText');
+    const bioContainer = document.getElementById('bioContainer');
+    if (profileData.bio) {
+      bioText.textContent = profileData.bio;
+      bioContainer.style.display = 'block';
+    } else {
+      if (isOwnProfile) {
+        bioText.textContent = 'No bio added yet.';
+        bioContainer.style.display = 'block';
+      } else {
+        bioContainer.style.display = 'none';
+      }
+    }
+    
+    if (isOwnProfile) {
+      document.getElementById('editBioBtn').style.display = 'inline-block';
+      document.getElementById('bioInput').value = profileData.bio || '';
+    }
     
     if (profileData.photoURL) {
       document.getElementById('profileAvatar').innerHTML = `<img src="${profileData.photoURL}" alt="Avatar">` + (isOwnProfile ? `<div class="profile-avatar-overlay">Update Photo</div>` : '');
@@ -247,32 +276,36 @@
     loadProfileReviews();
     loadProfileWatched();
 
-    if (isOwnProfile && window.loadWatchHistory) {
-      // Temporarily override renderContinueWatching
-      const oldRender = window.renderContinueWatching;
-      window.renderContinueWatching = (history) => {
-        const c = document.getElementById('userHistoryContainer');
-        if (!history || history.length === 0) {
-          c.innerHTML = '<div class="empty-state">No movies watched yet.</div>';
-          c.style.display = 'block';
-        } else {
-          c.style.display = 'grid';
-          c.innerHTML = history.map(m => `
-            <div class="movie-card" onclick="window.location.href='movie.html?id=${m.id}&type=${m.mediaType || 'movie'}'">
-              <div class="card-poster">
-                <img src="${m.poster}" alt="${m.title}"/>
-              </div>
-              <div class="card-info">
-                <h3>${m.title}</h3>
-              </div>
-            </div>
-          `).join('');
-        }
-      };
-      window.loadWatchHistory();
-    }
+
   }
 
+  window.toggleBioEdit = function() {
+    const viewDiv = document.getElementById('bioContainer');
+    const editDiv = document.getElementById('bioEditContainer');
+    if (viewDiv.style.display === 'none') {
+      viewDiv.style.display = 'block';
+      editDiv.style.display = 'none';
+    } else {
+      viewDiv.style.display = 'none';
+      editDiv.style.display = 'block';
+      document.getElementById('bioInput').focus();
+    }
+  };
+
+  window.saveBio = async function() {
+    const input = document.getElementById('bioInput').value.trim();
+    if (window.updateBio) {
+      const success = await window.updateBio(input);
+      if (success) {
+        document.getElementById('bioText').textContent = input || 'No bio added yet.';
+        showNotif('Bio updated successfully! Γ£¿');
+        toggleBioEdit();
+      } else {
+        showNotif('Failed to update bio. ≡ƒÿó');
+      }
+    }
+  };
+
   function switchTab(tabId, btn) {
     document.querySelectorAll('.ptab').forEach(b => b.classList.remove('active'));
     document.querySelectorAll('.tab-content').forEach(c => c.classList.remove('active'));
diff --git a/style.css b/style.css
index 0c3a271..1702d53 100644
--- a/style.css
+++ b/style.css
@@ -270,17 +270,6 @@ a{color:var(--gold);text-decoration:none}
 .review-card .rc-delete{background:none;color:var(--red);font-size:.7rem;margin-top:6px;padding:2px 8px;border-radius:8px;border:1px solid var(--red);transition:all .3s}
 .review-card .rc-delete:hover{background:var(--red);color:#fff}
 
-/* ===== VIDEO PLAYER ===== */
-.player-section{background:linear-gradient(135deg,rgba(229,9,20,.08),rgba(139,92,246,.08));border:1px solid rgba(229,9,20,.2);border-radius:var(--radius);padding:20px;margin-bottom:28px}
-.watch-btn{padding:12px 32px;border-radius:30px;background:linear-gradient(135deg,#e50914,#b20710);color:#fff;font-weight:700;font-size:.95rem;transition:all .3s;box-shadow:0 4px 20px rgba(229,9,20,.3)}
-.watch-btn:hover{transform:translateY(-3px) scale(1.03);box-shadow:0 8px 35px rgba(229,9,20,.5)}
-.series-controls{display:flex;gap:10px;flex-wrap:wrap;align-items:center}
-.series-select{padding:10px 16px;border-radius:12px;background:var(--card);border:1px solid var(--border);color:var(--text);font-size:.85rem;font-family:inherit;cursor:pointer;min-width:140px;transition:border-color .3s}
-.series-select:focus{outline:none;border-color:var(--gold)}
-.series-select option{background:var(--card);color:var(--text)}
-.player-wrap{margin-top:16px;border-radius:var(--radius);overflow:hidden;position:relative;padding-top:56.25%;background:#000;border:2px solid rgba(229,9,20,.3);box-shadow:0 10px 40px rgba(0,0,0,.5);animation:scaleIn .4s ease;-webkit-overflow-scrolling:touch;transform:translateZ(0)}
-.player-wrap.hidden{display:none}
-.video-iframe{position:absolute;top:0;left:0;width:100%;height:100%;border:none;pointer-events:auto;cursor:pointer;z-index:10;transform:translateZ(0)}
 
 /* ===== STREAMING & INDIA CINEMA HUBS ===== */
 .streaming-hub, .india-hub {
@@ -427,6 +416,16 @@ a{color:var(--gold);text-decoration:none}
 /* ===== LOADER ===== */
 .loader{display:flex;justify-content:center;padding:40px}.loader::after{content:'';width:36px;height:36px;border:3px solid var(--border);border-top-color:var(--gold);border-radius:50%;animation:spin .8s linear infinite}
 
+/* ===== PROFILE BIO ===== */
+.profile-bio-container { margin-top: 10px; }
+.profile-bio-text { color: var(--text2); font-size: 0.85rem; line-height: 1.5; margin-bottom: 8px; }
+.edit-bio-btn { background: none; border: 1px solid var(--border); color: var(--text); padding: 4px 10px; border-radius: 20px; font-size: 0.75rem; cursor: pointer; transition: all 0.3s; }
+.edit-bio-btn:hover { background: rgba(255,255,255,0.1); border-color: var(--text); }
+.profile-bio-edit { margin-top: 10px; }
+.bio-textarea { width: 100%; max-width: 400px; height: 80px; background: var(--card); border: 1px solid var(--border); color: var(--text); padding: 10px; border-radius: 8px; font-family: inherit; font-size: 0.85rem; resize: none; margin-bottom: 8px; }
+.bio-textarea:focus { outline: none; border-color: var(--gold); }
+.bio-actions { display: flex; gap: 8px; }
+
 /* ===== RESPONSIVE ===== */
 @media(max-width:768px){
   .navbar{position:sticky;top:0;background:var(--bg);padding:10px 16px;gap:8px;flex-wrap:wrap;border-bottom:1px solid var(--border)}
@@ -446,14 +445,11 @@ a{color:var(--gold);text-decoration:none}
   .hero-dots{right:20px}
   .nav-right{display:flex;gap:6px}
   .nav-btn{padding:5px 10px;font-size:0.75rem}
-  /* ===== PLAYER MOBILE FIX ===== */
   .modal-overlay{padding:20px 0}
   .modal{border-radius:12px 12px 0 0;width:100%;max-width:100%}
-  .player-section{padding:12px 0;border-radius:0;border-left:none;border-right:none;margin-left:-8px;margin-right:-8px;width:calc(100% + 16px)}
-  .player-wrap{border-radius:0;border-left:none;border-right:none;margin-left:-12px;margin-right:-12px;width:calc(100% + 24px);border-top:2px solid rgba(229,9,20,.4);border-bottom:2px solid rgba(229,9,20,.4)}
-  .series-controls{padding:0 12px}
 }
 @media(max-width:480px){
   .movies-grid{grid-template-columns:repeat(2,1fr);gap:10px}
   .hero-title{font-size:1.8rem}
 }
+
